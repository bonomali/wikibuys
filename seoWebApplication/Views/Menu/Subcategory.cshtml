@model IEnumerable<seoWebApplication.Models.Subcategories>

@foreach (var item in Model)
{
    <li>
        <a href="/Subcategory/@Linkor.GenerateSlug(item.Name.ToString())/" title="@Html.DisplayFor(modelItem => item.Description)">@Html.DisplayFor(modelItem => item.Description)</a>
    </li>
}



